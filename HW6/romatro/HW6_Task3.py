calculated=input("please input the string to calculate characters: ")
def char_number(str_to_count=calculated):
    calculated_string=list(calculated)
    print (len(calculated_string))

char_number()